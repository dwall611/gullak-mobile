/**
 * Gullak Mobile — Centralized Color Palette
 *
 * Re-exports from designTokens.js for convenience.
 * Import this if you only need colors; import designTokens.js for the full system.
 */

export {
  colors,
  surface,
  text,
  brand,
  semantic,
  category,
  categoryFallback,
  border,
} from './designTokens';
